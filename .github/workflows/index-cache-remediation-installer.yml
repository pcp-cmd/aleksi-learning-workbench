name: One-shot Windows qualification for index remediation

on:
  push:
    branches:
      - build/index-cache-remediation-20260919

permissions:
  actions: read
  contents: read
  id-token: write
  attestations: write

jobs:
  build-and-qualify:
    uses: ./.github/workflows/windows-qualification.yml
    permissions:
      actions: read
      contents: read
      id-token: write
      attestations: write
