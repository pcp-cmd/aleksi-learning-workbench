# V0.1 Acceptance

Status: frozen for V0.1

Each ID is stable and executable. Evidence comes from fresh automated tests,
filesystem inspection, browser assertions, or delivery verification.

## 1. Persistence and workflow acceptance

| ID | Preconditions | Action | Expected evidence |
|---|---|---|---|
| `ACC-READ-CREATE-01` | A fresh temporary Vault is initialized. | `POST /api/readings` with title, concept, Markdown body, and no ID/path/timestamp. | One UTF-8/LF Markdown file is created under `01-阅读材料/`; frontmatter contains server-generated ID/time; the response contains the actual save receipt; a filename collision creates `-2` without overwrite. |
| `ACC-READ-01` | The saved reading contains `$x_n \to a$` and one `$$...$$` block. | Open the reading in 数学阅读器. | Browser assertions find inline and block KaTeX; delimiters are not displayed as unparsed text; raw Markdown remains retrievable from disk. |
| `ACC-SELECT-01` | A reading is open with selectable prose. | Select a non-empty in-reader excerpt. | Exactly five actions appear: `生成定义卡`, `生成例子卡`, `生成反例卡`, `生成证明卡`, `记录卡点`. |
| `ACC-CARD-01` | Parameterize over `definition`, `example`, `counterexample`, and `proof`; each `CardCreateInput` contains its required type fields and a server-issued reading ID. | Create each card, reload its persisted `CardRecord`, then archive a separate copy. | Create defaults/omissions normalize exactly as contracted; each type writes to its fixed Chinese directory with canonical frontmatter/body units, Obsidian links, provenance fields initialized to null, and a server revision; response separates `card` from `SaveReceipt`; archive moves under `99-归档/` with mastery `archived`; no permanent delete occurs. |
| `ACC-CARD-UPDATE-01` | An active card exists with all editable fields populated. | Send a complete `CardUpdateInput` changing title and one value while explicitly clearing `relatedConcepts`, `understanding`, `blockType`, and `nextAction`; then separately omit each required key, send `null` for a non-nullable key, send `due`/`archived`, send `nextReview`, and send immutable/server-owned fields. | Complete PUT performs full editable-field replacement, keeps the existing `relativePath`, and appends one revision; `[]`, `""`, and `null` clear only their contracted fields. Every incomplete/invalid/unknown/server-owned request is rejected atomically with no Markdown/cache/revision change. |
| `ACC-WIRE-01` | Canonical fixtures exist for reading, all four cards, diagnosis, pending and committed ReviewRecords, and Codex task, including leading/trailing whitespace, CRLF input, optional empty fields, reserved-looking headings, backticks, and literal `<!-- /aleksi:value -->`. | Normalize input once, serialize, parse, and serialize every fixture. | Output is byte-stable UTF-8 without BOM, LF-only, one final LF, exact frontmatter/heading/body-unit order, exact `bytes=` counts, correct optional-section omission, no backslash heading escape, and canonical validated Obsidian links. |
| `ACC-WIRE-BYTES-01` | The inline normative ε-N reading fixture and normative `bytes=0` fixture from `DATA_SCHEMA.md` are available as expected bytes. | Compare serializer output byte-for-byte; parse and serialize again; separately serialize a required empty value; test forbidden concept/title values containing U+007C vertical bar, `]]`, CR/LF, `/`, and `\`. | Both non-empty serializations equal the exact fixture bytes, including its 49-byte value and final LF. Empty output equals `"## 量词结构\n<!-- aleksi:value bytes=0 -->\n\n<!-- /aleksi:value -->\n"` exactly: zero value bytes and one structural blank line; consecutive markers or two blank lines are rejected. Every forbidden link/title value is rejected before file creation. |
| `ACC-DIAG-01` | An initialized Vault and optional related card are available. | Save one diagnosis per `BlockType`; submit one unknown block. | Eight valid Markdown files persist under `06-卡点诊断/` with explicit server-generated `title`; the unknown block is rejected without a file. |
| `ACC-REVIEW-01` | A reviewable card has a known `nextReview`; each isolated submission has a fresh client-generated UUID v4 `idempotencyKey`. | Submit `forgot`, `fuzzy`, `known`, and `fluent`, each with a block type. | Each logical review has one deterministic ID, one committed ReviewRecord, one monotonic per-card `reviewSequence`, one review revision, and matching applied ID/sequence provenance; date advances exactly 1/3/7/14 UTC date-only days and mastery becomes `rebuild`/`learning`/`learning`/`mastered`. |
| `ACC-REVIEW-TXN-01` | Failure injection can stop the transaction at pending ReviewRecord replace, staged card replace, commit-marker replace, finalized card replace, and cache rebuild. | Inject each deterministic failure in an isolated Vault, restart/rebuild, inspect authoritative Markdown, then retry the same key. | Proven pre-commit failures ignore pending history, durably roll back staged card state, and rebuild to the pre-review authoritative state. At/after proven commit-marker durability there is exactly one committed history item; final-card/cache failures return or replay a committed result with stale/recovery evidence. Same-key retry preserves sequence/ID/result and creates no duplicate history or sequence; ambiguous marker outcomes are covered separately. |
| `ACC-REVIEW-INDETERMINATE-01` | Commit-marker replace can report an ambiguous result, and deterministic-path reread can independently fail. | Inject replace ambiguity plus reread failure, inspect the response, then retry first while reread still fails and again after the record is readable as pending or committed. | Initial and unresolved retries return `503 REVIEW_COMMIT_INDETERMINATE`, `retryable: true`, and the same review ID/key; they allocate no new ID, path, or sequence. Once readable, committed returns the original result, while pending resumes the same transaction. Pre-commit failure requires a successful reread proving pending plus durable rollback, or proven absence plus proven/restored base card. |
| `ACC-REVIEW-IDEMPOTENT-01` | One review key has committed successfully. | Repeat the identical request multiple times and concurrently; then reuse the key with different feedback or block type. | Identical retries return `200`, `replayed: true`, and the same canonical committed result; exactly one ReviewRecord path and revision exist. Changed payload returns `409 IDEMPOTENCY_KEY_REUSE` and changes nothing. |
| `ACC-REVIEW-SEQUENCE-01` | Server clock is fixed to one millisecond. Choose two idempotency keys whose deterministic ReviewRecord IDs sort in the opposite lexical order from submission/commit order. | Commit review A, then review B for the same card; delete caches and rebuild. | A has sequence 1 and B sequence 2 despite equal `reviewedAt` and reversed ID sort. History is A then B, `lastReviewSequence` is 2, `lastReviewed` comes from B, and card repair/application uses B. Restart recovers the next sequence as 3 from the maximum committed sequence. |
| `ACC-REVIEW-HISTORY-01` | A card has at least two committed ReviewRecords plus one pending record; cache is populated and applied sequence provenance is stale below the latest committed sequence. | Delete `.aleksi/review-queue.json`, rebuild, and reload. | Pending is excluded; committed history is ascending `reviewSequence`; `lastReviewSequence` is the maximum committed sequence and `lastReviewed` is that record's timestamp; only a committed sequence greater than `lastAppliedReviewSequence` repairs schedule/mastery/block; ID/sequence provenance advances together and `CARD_REVIEW_SUMMARY_REPAIRED` is reported once. |
| `ACC-REVIEW-MANUAL-01` | A committed review is applied to a card. | PUT a full card replacement that manually changes mastery and block type, delete all three caches, rebuild twice, then replay the old review key. | PUT sets `reviewOverrideAt`; both rebuilds preserve the manual mastery/block and create no review revision; replaying the old key does not reapply it. A separately submitted later review key may supersede the manual values and clears the override. |
| `ACC-GRAPH-01` | Fixed clock is `2026-06-22T03:14:15.926Z`; the Vault contains exactly one active definition card for `ε-N`, no other active cards, and no enriching diagnosis. | Delete graph cache, rebuild, and read the complete JSON. | JSON equals the normative complete ε-N object below, not merely a partial match. |
| `ACC-GRAPH-02` | Fixtures contain active/archived cards, readings, diagnoses with and without card-created concepts, pending/committed reviews, Codex tasks, same-time diagnoses with different IDs, stored next actions, due cards, duplicate related concepts, and multiple concept names. | Rebuild graph twice from identical Markdown with caches removed. | Only active-card concepts create nodes; non-card assets only enrich as contracted; both outputs have identical node order, ring precedence, diagnosis tie-break, next-action precedence, all exact missing/rebuild/due Chinese strings in priority order, deduplicated suggestions, and related edges only when both nodes exist. |
| `ACC-CODEX-01` | Concept/material/understanding/block are available; optional source IDs are valid. | Generate a Codex task. | A date-prefixed Markdown file under `09-Codex任务/` has an explicit server title, exact heading order, five fixed requested actions, learning guardrail, and no automatic execution. |

Normative complete expected JSON for `ACC-GRAPH-01`:

```json
{
  "generatedAt": "2026-06-22T03:14:15.926Z",
  "concepts": {
    "ε-N": {
      "concept": "ε-N",
      "rings": {
        "definition": {
          "count": 1,
          "state": "established"
        },
        "example": {
          "count": 0,
          "state": "missing"
        },
        "counterexample": {
          "count": 0,
          "state": "missing"
        },
        "proof": {
          "count": 0,
          "state": "missing"
        }
      },
      "currentBlock": null,
      "nextAction": "补 1 张例子卡",
      "hasDueReview": false,
      "relatedConcepts": [],
      "suggestedNextActions": [
        "补 1 张例子卡",
        "补 1 张反例卡",
        "补 1 张证明卡"
      ]
    }
  }
}
```

## 2. Authority, safety, recovery, and settings

| ID | Preconditions | Action | Expected evidence |
|---|---|---|---|
| `ACC-SAFE-01` | The API uses a temporary Vault and an outside sentinel directory. Parameterize over `POST /api/readings`, `POST /api/cards`, `PUT /api/cards/:id`, `POST /api/cards/:id/archive`, `POST /api/diagnoses`, `POST /api/review/:id/result`, `POST /api/codex/tasks`, and `POST /api/index/rebuild`. | For every route, add one unknown key and inject applicable body `id`, route-selector ID, `createdAt`, `reviewedAt`, `relativePath`, `absolutePath`, `filename`, `revisionLog`, and provenance fields. On card create/PUT/review also inject `nextReview`; send create `mastery`, PUT `mastery` values `due` and `archived`, and body IDs. Supply unresolved client-minted references; attempt traversal, encoded separators, absolute output paths, and symlink escape. | Each strict schema rejects every unknown/immutable/server-owned/body-ID case without mutation. Card create rejects all mastery; card PUT accepts only `learning`/`mastered`/`rebuild` and rejects `nextReview`; review accepts only `idempotencyKey`/`feedback`/`blockType`; archive/index accept only their exact confirmation bodies. Valid route selectors address only resolved assets; no outside file changes. |
| `ACC-VAULT-SAFE-01` | Isolated app settings, source/destination Vaults, and outside sentinels exist. | Exercise initialize/select with exact path-only bodies after their dialog actions; add body `confirmed` and other unknown fields to each. Exercise migrate/backup with `confirmed: true`, then omit it and send `false`. For all path routes, use ambiguous/traversal/symlink locations and attempt asset fields. | Initialize/select accept no body confirmation field; their dialog action is out-of-band. Migrate/backup require literal `confirmed: true`. Every invalid body changes neither locator nor Vaults/backups; the narrow absolute-path allowance does not apply to asset routes. |
| `ACC-RECOVER-01` | Parameterize over `index.json`, `review-queue.json`, and `graph-state.json`; authoritative Markdown exists; a same-millisecond corrupt backup name is pre-created. | Replace each cache with invalid JSON and separately schema-invalid JSON, then read/rebuild it. | Each cache is renamed using filesystem-safe UTC; collision uses `-2`; a valid cache rebuilds from Markdown; review history/`lastReviewed` survives; response reports recovery; authoritative Markdown is unchanged. |
| `ACC-SETTINGS-01` | Isolated app-settings directory plus separate initialize, select, and migration Vault paths exist. | Through the Settings dialog: initialize a Vault, restart; select a different valid Vault, restart; migrate to an empty destination, restart; run backup, restart. Read both settings files after each operation. | Only initialize/select/migrate change `activeVaultPath`, and each survives restart. `.aleksi/settings.json` contains exactly ordered `schemaVersion: 1` and immutable `vaultId`; select and backup do not rewrite it; migration preserves a valid source `vaultId` or creates one only for a legacy source after verified copy. No general settings-update route/request occurs. |
| `ACC-BACKUP-01` | A writable active Vault and an existing same-millisecond backup directory are present. | Record app locator and Vault settings bytes, confirm Vault backup twice, then restart. | Backups use `Aleksi-Learning-Vault-backup-<utcStamp>` then `-2`; both are outside the live Vault; neither prior backup is overwritten; active path and live `.aleksi/settings.json` bytes are unchanged and restart reopens the same Vault. |

## 3. UI acceptance

Primary viewport for these IDs is `1440 × 900`, device scale factor `1`.

| ID | Preconditions | Action | Expected evidence |
|---|---|---|---|
| `ACC-UI-01` | The app is open at `/today`. | Visit `/today`, `/reader`, `/cards`, `/diagnosis`, `/review`, `/graph`; open and close one drawer. | Six Chinese route names appear in an `80px` rail; workspace starts at `x=80`; the `72px` bottom band has exactly three equal cells; closed drawer is absent; open drawer is a `400px` right overlay. |
| `ACC-UI-02` | Reference ZIP hash and token stylesheet are available. | Recompute the ZIP SHA-256 and inspect source/computed styles for cards, rows, buttons, dots, reader, and graph. | Hash equals the frozen value; every listed token matches, including `--radius-sm: 10px`, `--radius-md: 16px`, and `--radius-lg: 24px`; cards have `1px` borders and no shadow/glow; hover is `-1px` or none; status dots are `8px` with adjacent Chinese text; reader measure is at most `760px`. |
| `ACC-UI-03` | Browser reduced-motion emulation is available. | Navigate routes, open/close drawer, hover/select rows, and focus controls with reduced motion enabled. | Transforms are none; durations are `0ms` or at most `1ms`; content/state/focus remain visible; no motion gates content. |
| `ACC-UI-04` | Normal motion is enabled. | Time route and drawer transitions and exercise focus/keyboard behavior. | Route uses `8px → 0` vertical motion and drawer `16px → 0` horizontal motion; both complete within `250ms`; route content is visible within `300ms`; `Escape` closes drawer and focus returns to trigger. |
| `ACC-UI-05` | Demonstration data exists on all routes. | Inspect route labels, forms, empty states, save feedback, and graph details. | User-visible headings/fields are Chinese; no hero, exhibition wall, public copy, glow/neon treatment, large zoom, or long loop appears. |

## 4. Explicit boundary acceptance

These checks use inventories and behavior, not keyword presence alone.

| ID | Preconditions | Action | Expected evidence |
|---|---|---|---|
| `ACC-NONGOAL-01` | Server route registrations and dependency manifests are available. | Enumerate routes and compare with the approved 18-route set; inspect direct dependencies/imports for `electron`, `openai`, `@anthropic-ai/sdk`, `pdfjs-dist`, `pdf-parse`, `tesseract.js`, `firebase`, and `@supabase/supabase-js`; intercept network requests. | Route set is exact with no auth/account/chat/sync endpoint; denylisted dependencies/imports are absent; runtime requests target only `127.0.0.1`. |
| `ACC-NONGOAL-02` | 飞轮图谱 contains concept nodes. | Inspect event handlers and attempt pointer/keyboard movement, edge creation, and zoom persistence. | Nodes only open details; there is no drag handler, edge-creation control, topology mutation route, or persisted custom layout. |
| `ACC-NONGOAL-03` | Filesystem calls are instrumented; an unrelated Obsidian Vault exists. | Exercise reading/card/diagnosis/review/Codex/settings/archive/migrate/backup flows and attempt the most destructive card action. | Writes occur only inside the active Vault, the app-settings file, an explicitly confirmed empty migration destination, or an explicitly confirmed backup/Desktop destination; no delete route exists; archive moves under `99-归档/`; unrelated Vault is neither scanned nor modified. |
| `ACC-NONGOAL-04` | Codex candidate workflow is available. | Generate a task and paste sample returned content. | No external model request occurs; returned text remains candidate content; only explicit field-copy plus save changes Markdown. |

Approved route set for `ACC-NONGOAL-01`:

```text
GET    /api/vault/status
POST   /api/vault/initialize
POST   /api/vault/select
POST   /api/vault/migrate
POST   /api/vault/backup
POST   /api/readings
GET    /api/readings
GET    /api/readings/:id
POST   /api/cards
GET    /api/cards/:id
PUT    /api/cards/:id
POST   /api/cards/:id/archive
POST   /api/diagnoses
GET    /api/review/today
POST   /api/review/:id/result
GET    /api/graph/state
POST   /api/codex/tasks
POST   /api/index/rebuild
```

## 5. Desktop delivery acceptance

| ID | Preconditions | Action | Expected evidence |
|---|---|---|---|
| `ACC-DESKTOP-01` | Workspace static/unit/API/browser/real-write/visual checks pass. Destination is absent, or replacement is explicitly confirmed. | If present, rename the whole destination to `aleksi-learning-workbench.previous-<utcStamp>` with collision suffix; copy the fresh project excluding the six fixed directories; generate `DESKTOP_PACKAGE_MANIFEST.json`; run Desktop verification; finally re-enumerate and rehash the destination. | Destination is `C:\Users\pcp\Desktop\aleksi-learning-workbench`; no silent merge/overwrite occurs. Every manifest file exists exactly once and its recomputed SHA-256/byte size matches; the actual copied-file set equals the manifest set with no missing or unlisted file after excluding only the manifest itself and verification-generated `node_modules/**`. All six excluded directories remain absent; service binds to `127.0.0.1`; a Desktop-package card writes/reloads; reconciliation failure rejects delivery. |

## 6. Traceability

| Contract rule | Acceptance IDs |
|---|---|
| Reading creation and KaTeX | `ACC-READ-CREATE-01`, `ACC-READ-01` |
| Exactly five selection actions | `ACC-SELECT-01` |
| Separate create/update/record/receipt card schemas and archive | `ACC-CARD-01`, `ACC-CARD-UPDATE-01` |
| Reversible canonical Markdown and link/filename validation | `ACC-WIRE-01`, `ACC-WIRE-BYTES-01` |
| Diagnosis enum and explicit title | `ACC-DIAG-01`, `ACC-WIRE-01` |
| Durable/idempotent review transaction and mastery transitions | `ACC-REVIEW-01`, `ACC-REVIEW-TXN-01`, `ACC-REVIEW-INDETERMINATE-01`, `ACC-REVIEW-IDEMPOTENT-01` |
| Monotonic review ordering and provenance | `ACC-REVIEW-SEQUENCE-01`, `ACC-REVIEW-HISTORY-01` |
| Manual post-review authority | `ACC-REVIEW-MANUAL-01` |
| Markdown authority and cache recovery | `ACC-REVIEW-TXN-01`, `ACC-REVIEW-HISTORY-01`, `ACC-RECOVER-01` |
| Client/server authority and Vault containment | `ACC-SAFE-01`, `ACC-VAULT-SAFE-01`, `ACC-NONGOAL-03` |
| Deterministic graph aggregation | `ACC-GRAPH-01`, `ACC-GRAPH-02` |
| Codex task-only workflow | `ACC-CODEX-01`, `ACC-NONGOAL-04` |
| App and Vault settings | `ACC-SETTINGS-01`, `ACC-BACKUP-01` |
| Frozen Aleksi visual reference and layout | `ACC-UI-01` through `ACC-UI-05` |
| Explicit V0.1 exclusions | `ACC-NONGOAL-01` through `ACC-NONGOAL-04` |
| Desktop package manifest and overwrite policy | `ACC-DESKTOP-01` |

## 7. V0.1 delivery evidence log

Recorded through: `2026-06-29T12:49:14.893Z`

Branch: `feature/workbench-v0.1`

Desktop destination:
`C:\Users\pcp\Desktop\aleksi-learning-workbench`

Desktop copy result:

- Destination was absent before copy; no prior folder was overwritten or
  renamed.
- Copied 106 source/package files.
- Excluded from the initial copy: `.git`, `node_modules`, `dist`,
  `playwright-report`, `test-results`, `.superpowers`.

Workspace verification:

- Command: `npm.cmd run verify`
- Start UTC: `2026-06-29T12:48:47.461Z`
- End UTC: `2026-06-29T12:49:14.893Z`
- Exit code: `0`
- Result: typecheck passed; Vitest reported 21 passed test files, 224 passed
  tests, and 1 skipped symlink-permission case
  (`OS denied file symlink creation: EPERM`); Vite production build completed.

Browser demonstration verification:

- Command: `npm.cmd run test:browser`
- Start UTC: `2026-06-29T12:48:13.252Z`
- End UTC: `2026-06-29T12:48:22.410Z`
- Exit code: `0`
- Result: 1 Chromium test passed:
  `tests\browser\epsilon-n-flow.spec.ts:114:1` completed the ε-N learning loop
  and reloaded persisted state in `7.8s`.

Desktop package verification:

- Command:
  `powershell.exe -NoProfile -ExecutionPolicy Bypass -File "C:\Users\pcp\Desktop\aleksi-learning-workbench\scripts\verify-desktop-package.ps1" -Path "C:\Users\pcp\Desktop\aleksi-learning-workbench"`
- Exit code: `0`
- Manifest:
  `C:\Users\pcp\Desktop\aleksi-learning-workbench\DESKTOP_PACKAGE_MANIFEST.json`
- Manifest generated UTC: `2026-06-29T12:45:17.314Z`
- Manifest file count: `106`
- `npm install` result: added 403 packages and audited 404 packages; npm
  reported 2 moderate audit findings without failing the install.
- Desktop `npm.cmd run verify` result inside the verifier: typecheck passed;
  Vitest reported 21 passed test files, 224 passed tests, and 1 skipped
  symlink-permission case; Vite production build completed; verifier removed
  generated `dist` afterward.
- Local service health URL: `http://127.0.0.1:52876/api/health`
- Temporary Desktop-package Vault:
  `C:\Users\pcp\AppData\Local\Temp\aleksi-desktop-package-4718168c1f1440368c8b06515756ccb7\Vault`
- Created/reloaded card ID: `b77f6bec-bbd2-46a4-86cd-e0b5c87893ab`
- Observed save receipt:
  - `relativePath`: `02-定义卡/Desktop package definition.md`
  - `modifiedAt`: `2026-06-29T12:45:58.922Z`
- Final package reconciliation: manifest still matched; `node_modules` was
  present only as verification-generated dependency output; `.git`, `dist`,
  `playwright-report`, `test-results`, and `.superpowers` were absent.
